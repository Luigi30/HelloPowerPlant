#ifndef SHAPES_H#define SHAPES_H#pragma once on#include <UDrawingState.h>#include <QuickDraw.h>#include <MacTypes.h>#include <vector>#include ":3D:Vector3f.h"#include "ShapeHelper.h"const Point OFFSET_NONE = {0, 0};class Shape2D {	std::vector<Point> _vertices;	SInt16 rotationDegrees;		public:	int GetVertexCount();	void Draw(Point offset);		void AddVertex(Point v);	void AddVertex(short v, short h);		SInt16 GetRotationDegrees();	void SetRotationDegrees(SInt16 degrees);};#endif